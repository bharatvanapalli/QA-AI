# Dependencies
node_modules/
.pnp.*

# Build outputs
dist/
build/
.vite/

# Logs
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment & secrets — NEVER commit these
.env
.env.local
.env.*.local
.env.development
.env.production

# Local SQLite DB — contains encrypted Claude/Gemini API keys + user data
prisma/dev.db
prisma/dev.db-journal
prisma/*.db
prisma/*.db-journal
prisma/*.sqlite
prisma/*.sqlite-journal
dev.db
*.db
*.sqlite

# Playwright runtime artifacts (per-run, not source)
playwright/test-results/
playwright/playwright-report/
playwright/tests/
playwright/pages/
playwright/fixtures/
playwright/utils/
playwright/.cache/

# Generated playwright scaffolding (rewritten on first conductor run)
playwright/playwright.config.js
playwright/playwright.config.ts
playwright/package.json
playwright/tsconfig.json
playwright/node_modules/

# Server-side debug / one-shot scripts output
server/scripts/*.output.json

# Playwright MCP subprocess logs (per-session, not source)
.playwright-mcp/

# Debug screenshots / one-off captures at repo root
/tc*.png
/screenshot-*.png
/debug-*.png
/register-*.png
/registration-*.png

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# Coverage
coverage/
*.lcov
.nyc_output/

# Claude Code local context (per-machine)
.claude/
