{
  "name": "qaai-replayir-export",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "playwright test",
    "list": "playwright test --list"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
