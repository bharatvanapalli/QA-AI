const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function check() { const project = await prisma.project.findFirst({ where: { name: { contains: 'LetCode' } } }); const runs = await prisma.runResult.findMany({ where: { testCase: { projectId: project.id } }, orderBy: { createdAt: 'desc' }, take: 1 }); const run = runs[0]; const steps = typeof run.stepResults === 'string' ? JSON.parse(run.stepResults) : run.stepResults; console.log(JSON.stringify(steps[2], null, 2)); } check().finally(() => prisma.$disconnect());
